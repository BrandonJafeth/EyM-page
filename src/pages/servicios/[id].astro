---
import Layout from '../../layouts/Layout.astro';
import servicesData from '../../data/services.json';
import CTASection from '../../components/common/CTASection.astro';
import SectionTitle from '../../components/common/SectionTitle.astro';
import Icon from '../../components/common/Icon.astro';

export async function getStaticPaths() {
  const { detailed_services } = servicesData;
  return detailed_services.map((service) => ({
    params: { id: service.id },
    props: { service },
  }));
}

const { service } = Astro.props;
const { title, fullDescription, features } = service;

// Function to get a related background image (or use a default)
// You might want to map specific images to IDs later
const heroBg = "bg-primary"; 
---

<Layout 
  title={`${title} en Guanacaste - Asesoría Legal`} 
  description={`${title} en Liberia y Cañas. ${fullDescription.substring(0, 150)}... Contáctanos en EM & Asociados.`}
>
  <!-- Hero Section -->
  <section class={`relative py-20 md:py-32 ${heroBg} text-white overflow-hidden`}>
    <div class="absolute inset-0 z-0 opacity-20">
       <!-- Pattern or abstract background could go here -->
       <div class="absolute right-0 top-0 w-96 h-96 bg-accent rounded-full filter blur-[100px] translate-x-1/2 -translate-y-1/2 opacity-30"></div>
       <div class="absolute left-0 bottom-0 w-64 h-64 bg-secondary rounded-full filter blur-[80px] -translate-x-1/3 translate-y-1/3 opacity-20"></div>
    </div>
    
    <div class="container mx-auto px-6 relative z-10 text-center">
      <h1 class="font-heading text-4xl md:text-5xl lg:text-6xl mb-6 fade-in-up">
        {title}
      </h1>
      <div class="w-24 h-1 bg-accent mx-auto mb-6 fade-in-up delay-100"></div>
      <p class="font-body text-lg text-gray-300 max-w-2xl mx-auto fade-in-up delay-200">
        Servicios legales expertos en Guanacaste, Liberia y Cañas.
      </p>
    </div>
  </section>

  <!-- Content Section -->
  <section class="py-16 md:py-24 bg-white">
    <div class="container mx-auto px-6">
      
      <!-- Back Link -->
      <div class="mb-12 fade-in-section">
        <a 
          href="/servicios" 
          class="inline-flex items-center text-primary hover:text-accent transition-colors duration-300 font-heading tracking-widest text-sm uppercase group"
        >
          <span class="mr-2 transform group-hover:-translate-x-1 transition-transform duration-300">←</span>
          Volver a Servicios
        </a>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-20">
        
        <!-- Main Content -->
        <div class="lg:col-span-7 fade-in-section">
          <h2 class="font-heading text-3xl text-primary mb-6">Descripción del Servicio</h2>
          <div class="prose prose-lg text-gray-600 font-body leading-relaxed text-justify">
            <p>{fullDescription}</p>
            
            <p class="mt-6">
              En <strong>EM & Asociados</strong>, entendemos que cada caso es único. Nuestro equipo de abogados expertos en {title} está preparado para brindarle la asesoría personalizada que necesita para proteger sus intereses y alcanzar sus objetivos.
            </p>
             <p class="mt-4">
              Prestamos nuestros servicios en toda la provincia de Guanacaste, con oficinas centrales en Liberia y presencia en Cañas.
            </p>
          </div>
        </div>

        <!-- Sidebar / Features -->
        <div class="lg:col-span-5 fade-in-section delay-100">
          <div class="bg-gray-50 p-8 rounded-lg border border-gray-100 shadow-sm sticky top-32">
            <h3 class="font-heading text-2xl text-primary mb-6 border-b border-gray-200 pb-4">
              ¿Qué incluye este servicio?
            </h3>
            
            <ul class="space-y-4">
              {features.map((feature) => (
                <li class="flex items-start">
                  <div class="mt-1 text-accent mr-3">
                     <!-- Simple Check Icon -->
                     <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
                  </div>
                  <span class="text-gray-700 font-body text-lg">{feature}</span>
                </li>
              ))}
            </ul>

            <div class="mt-10 pt-6 border-t border-gray-200">
               <p class="text-sm text-gray-500 mb-4 font-body italic">
                 ¿Tiene dudas sobre si este servicio aplica a su caso?
               </p>
               <a 
                 href="/contacto" 
                 class="block w-full text-center bg-primary text-white font-heading uppercase tracking-widest py-3 px-6 hover:bg-accent transition-colors duration-300"
               >
                 Agendar Asesoría
               </a>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <CTASection />

</Layout>
